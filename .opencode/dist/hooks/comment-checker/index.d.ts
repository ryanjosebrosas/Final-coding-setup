/**
 * Comment Checker Hook
 *
 * Checks files for AI-generated comments and provides feedback.
 * Detects patterns like "// Generated by", "// AI-", etc.
 */
interface ToolExecuteInput {
    tool: string;
    sessionID: string;
    callID: string;
}
interface ToolExecuteOutput {
    title: string;
    output: string;
    metadata: unknown;
}
/**
 * Create the comment checker hook.
 */
export declare function createCommentCheckerHooks(config?: {
    enabled?: boolean;
}): {
    "tool.execute.before": (input: ToolExecuteInput, output: {
        args: Record<string, unknown>;
    }) => Promise<void>;
    "tool.execute.after": (input: ToolExecuteInput, output: ToolExecuteOutput) => Promise<void>;
};
export {};
